# Colours are hex

This is a technical spec, not a user story: it has no user-facing behaviour.

## Problem

`Node.colour` is an `Option<u8>` index into the palette, not the colour itself.

## Acceptance Criteria

## Technical Design
