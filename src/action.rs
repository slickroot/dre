#[derive(Clone, Copy, PartialEq, Eq, Debug)]
pub(crate) enum Action {
    Undo,
    NewBox,
    NewSibling,
    Delete,
    Paste,
    SelectParent,
    SelectChild,
    SelectNext,
    SelectPrevious,
    EditLabel,
    RenameLabel,
    CycleColour,
    CycleSiblingsColour,
    ToggleSiblingsFill,
    ToggleFill,
    ToggleRounded,
    Quit,
    ScrollBy(i64),
    Digit(u8),
    CancelCount,
    Commit,
    CommitAndAddChild,
    InsertBackspace,
    InsertAppend(char),
    Confirm,
    Cancel,
    SavePromptBackspace,
    SavePromptAppend(char),
}
